# Keep shell scripts LF so they run under Git Bash regardless of core.autocrlf
*.sh        text eol=lf
*.omp.json  text eol=lf

# PowerShell tolerates either; normalize to CRLF on Windows checkouts
*.ps1       text eol=crlf
*.md        text
*.json      text
